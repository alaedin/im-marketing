spring:
  application:
    name: discovery-service
  cloud:
    config:
      uri:
      - http://config-server:10002
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:10002